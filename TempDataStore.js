const mongoose = require('mongoose');
const User = require('@/../../Models/User');
const Recognition = require('@/../../Models/Recongnition');

const tempDataStore = {
    users:[
        
    ],
    recognitions: [
        
    ]
};

export default tempDataStore;